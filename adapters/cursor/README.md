# Adapter: Cursor

*Placeholder — install notes, not production integration.*

Cursor supports MCP servers. Point it at the local `lex-provenance-mcp` command (after
`pip install -e ".[mcp]"`) and the read-only `lexprov.*` tools become available in-editor.

*A fuller, tested adapter example lands in v0.2.*
