"""Single source of truth for the connector's public identity.

To rename the project (e.g. to provenance-law / attested-law / corpus-lex),
change the values here and rename the top-level folder. Nothing else in the
codebase hard-codes the name.
"""

# Brand / repo identity ------------------------------------------------------
BRAND_NAME = "LexProvenance"
REPO_SLUG = "lex-provenance-mcp"
TOOL_NAMESPACE = "lexprov"  # prefix for MCP tool names, e.g. lexprov.search_corpus
PACKAGE_NAME = "lex_provenance_mcp"

# One-line positioning (method-level, subject-agnostic on purpose) -----------
TAGLINE = "Verifiable, provenance-first legal corpora for AI systems."

# Version --------------------------------------------------------------------
VERSION = "0.1.0"

# Owner (fill in your legal name / entity before public release) -------------
OWNER = "dacheah"
